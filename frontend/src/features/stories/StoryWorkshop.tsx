import React from 'react';
import { Box, Typography } from '@mui/material';

const StoryWorkshop: React.FC = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Story Workshop</Typography>
            <Typography variant="body1">Feature coming soon.</Typography>
        </Box>
    );
};

export default StoryWorkshop;
