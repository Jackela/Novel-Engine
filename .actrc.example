# Act CLI Configuration (Example)
# Copy this file to .actrc and add your secrets

# Platform mapping for ubuntu-22.04
-P ubuntu-22.04=catthehacker/ubuntu:full-22.04

# Container architecture
--container-architecture linux/amd64

# Secrets file (create .secrets with GITHUB_TOKEN=your_token)
--secret-file .secrets

# Environment variables file
--env-file .env.act

# Performance options
--artifact-server-path ./act-artifacts
--network host
--reuse

# Usage:
# 1. Copy this file: cp .actrc.example .actrc
# 2. Create .secrets file with your GitHub token
# 3. Run: act -W .github/workflows/test-validation.yml
