# .ai-guard — AI 禁区清单（Novel-Engine）

## 只读（任何情况下不得修改）
- .env*
- config/env/*
- data/*.sqlite3
- data/backups/*
- server/drizzle/meta/*（迁移只能经 `pnpm --dir server db:generate` 生成）
- server/qa-baselines/（OpenAPI 快照只能经 `pnpm --dir server openapi:snapshot` 再生）
- AUDIT_REPORT_Linus.md

## 修改前必须单独确认
- package.json / pnpm-lock.yaml
- README.md
- compose.yaml
- Dockerfile
- Makefile / justfile

## 编码红线
- 禁止吞掉非预期错误；只捕获特定 transport/parsing/value/domain 异常
- 禁止 SQL/FTS5 字符串拼接（参数化或经 buildFtsMatchQuery 严格归约）
- 禁止删除 raise / assert / validate / sanitize / escape / auth / permission 相关代码
- 禁止引入 import 期数据库句柄、Fastify app 实例或魔法 proxy
- 禁止引入新依赖（pnpm add）
- 禁止修改测试文件（除非审计明确要求）
- 保持函数小体积（file-size gate 强制）；编排逻辑须拆分
- React 组件不超过 200 行；编排拆分到 hooks/components

## 执行约束
- 一个任务只对应一个审计 finding 或一个小 feature
- 修改前必须运行相关测试并确认通过
- 修改后必须再次运行测试并 `git diff` 复查
- 优先 surgical fix，禁止重写整个模块
- 每个 Finding 独立处理，禁止跨 Finding 传递上下文
