name: Frontend CI (Basic)

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (cached)
        if: env.ACT != 'true' && hashFiles('frontend/package-lock.json') != ''
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: ${{ env.ACT != 'true' && 'npm' || '' }}
          cache-dependency-path: ${{ env.ACT != 'true' && 'frontend/package-lock.json' || '' }}

      - name: Setup Node (no cache under act)
        if: env.ACT == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate package-lock for act
        if: env.ACT == 'true'
        working-directory: frontend
        run: npm install --package-lock-only

      - name: Install
        if: ${{ hashFiles('frontend/package.json') != '' }}
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Test (if present)
        if: ${{ hashFiles('frontend/package.json') != '' }}
        run: npm test --if-present --silent

      - name: Build
        if: ${{ hashFiles('frontend/package.json') != '' }}
        run: npm run build --if-present

      - name: Install Playwright Browsers
        if: ${{ hashFiles('frontend/package.json') != '' }}
        run: npx playwright install --with-deps chromium

      - name: Playwright Smoke Tests
        if: ${{ hashFiles('frontend/package.json') != '' && github.event_name == 'pull_request' }}
        run: npm run test:e2e:smoke

      - name: Playwright Full Tests
        if: ${{ hashFiles('frontend/package.json') != '' && github.event_name != 'pull_request' }}
        run: npm run test:e2e

      - name: Upload Experience Report
        if: ${{ always() && env.ACT != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: experience-report
          path: frontend/reports/experience-report-*.md
          if-no-files-found: ignore
