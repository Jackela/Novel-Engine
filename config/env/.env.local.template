# ACT Local Secrets Configuration Template
# Copy this file to .env.local and replace with actual values
# cp .env.local.template .env.local

# IMPORTANT: .env.local is in .gitignore and should NEVER be committed

# GitHub Personal Access Token (PAT) for ACT
# Required for cloning GitHub Actions from marketplace
# 
# To create a token:
# 1. Go to https://github.com/settings/tokens/new
# 2. Select scopes:
#    - repo (for private repositories)
#    - OR public_repo (for public repositories only)
# 3. Generate token
# 4. Copy and paste below
#
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Optional: Add other secrets for testing specific workflows
# ANTHROPIC_API_KEY=sk-ant-your_key_here
# AWS_ACCESS_KEY_ID=your_aws_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret
