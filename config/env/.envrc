# Resolve project root from this file's location (config/env/.envrc -> project root)
# so CODEX_HOME stays portable across machines and is never a hard-coded host path.
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
export CODEX_HOME="${PROJECT_ROOT}/.codex"
