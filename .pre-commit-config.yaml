minimum_pre_commit_version: "3.0.0"
fail_fast: true
default_install_hook_types:
  - pre-commit
  - pre-push

repos:
  - repo: local
    hooks:
      - id: git-diff-cached-check
        name: git diff --cached --check
        entry: git diff --cached --check
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: ruff-check
        name: ruff check
        entry: uvx --from ruff==0.15.13 ruff check src tests
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: uv-lock-check
        name: uv lock --check
        entry: uv lock --check
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: ssot
        name: SSOT alignment
        entry: python scripts/qa/check_ssot.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: repo-hygiene
        name: repository hygiene
        entry: python scripts/qa/check_repo_hygiene.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: openspec
        name: OpenSpec strict validation
        entry: corepack pnpm spec:validate
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: backend-pytest
        name: backend pytest
        entry: python -m pytest -q
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: openapi-snapshot
        name: OpenAPI snapshot
        entry: python scripts/qa/check_openapi_snapshot.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: frontend-type-check
        name: frontend type-check
        entry: corepack pnpm --dir frontend type-check
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: frontend-test
        name: frontend test
        entry: corepack pnpm --dir frontend test:unit
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: frontend-build
        name: frontend build
        entry: corepack pnpm --dir frontend build
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
