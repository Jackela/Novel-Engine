# Ralph Progress Log

## Codebase Patterns
- Use `from src.core.result import Ok, Err, Result` for Result pattern
- Value objects use `@dataclass(frozen=True)` with `__post_init__` for validation
- Test files need `pytestmark = pytest.mark.unit` at module level
- Mock `aioredis` in tests: `sys.modules["aioredis"] = MagicMock()`
- Import path pattern: `from src.contexts.<context>.domain.value_objects.<name> import <Class>`
- Domain events can accept Union types for backward compatibility (datetime | str)
- from_dict class methods should handle backward compatibility for renamed fields
- Two app factories exist: src.api.main_api_server:create_app and src.api.app:create_app - routers must be registered in both
- Tests use `import api_server` which loads src.api.app, not src.api.main_api_server
- Error responses use ErrorDetail schema with 'code', 'message', 'details' fields (not 'detail')
- Frontend TanStack Query hooks pattern: use queryClient.invalidateQueries for cache invalidation, onMutate/onError/onSettled for optimistic updates
- Frontend API hooks are in frontend/src/lib/api/*.ts, feature-specific hooks can go in frontend/src/features/*/api/
- Frontend components use shadcn/ui from @/shared/components/ui/ (Card, Button, etc.)
- E2E tests import Page type from '@playwright/test' at top of file, not inline

Started: Mon Feb 16 16:36:42 CST 2026
---

## Mon Feb 16 16:40 CST 2026 - SIM-001
- Created WorldCalendar value object with full functionality
- Implemented: advance(), format(), from_datetime(), validate(), to_dict(), from_dict(), total_days(), days_until()
- Files changed:
  - src/contexts/world/domain/value_objects/__init__.py (new)
  - src/contexts/world/domain/value_objects/world_calendar.py (new)
  - tests/unit/contexts/world/domain/value_objects/__init__.py (new)
  - tests/unit/contexts/world/domain/value_objects/test_world_calendar.py (new)
- **Learnings for future iterations:**
  - The world context value_objects directory had no __init__.py, needed to create one
  - Test directory structure follows src structure: tests/unit/contexts/world/domain/value_objects/
  - Result pattern uses Ok(value) and Err(exception) from src.core.result
  - When advancing days, the calculation uses while loop for overflow handling (day > days_per_month)
  - Mypy errors in coordinates.py are pre-existing (not from new code)
---

## Mon Feb 16 16:55 CST 2026 - SIM-002
- Integrated WorldCalendar into WorldState aggregate
- Replaced world_time: datetime field with calendar: WorldCalendar
- Updated advance_time() to use calendar.advance() and return Result type
- Added from_datetime_world_time() class method for migration from old format
- Added from_dict() class method with backward compatibility for old world_time field
- Updated WorldStateChanged.time_advanced to accept datetime | str
- Files changed:
  - src/contexts/world/domain/aggregates/world_state.py (modified)
  - src/contexts/world/domain/events/world_events.py (modified)
  - tests/unit/contexts/world/domain/aggregates/__init__.py (new)
  - tests/unit/contexts/world/domain/aggregates/test_world_state.py (new)
- **Learnings for future iterations:**
  - WorldState.aggregate now uses calendar field instead of world_time
  - from_dict() should check for both new field name and old field name for backward compat
  - Domain event factory methods may need Union types when changing from datetime to custom types
  - Test directory needed to be created: tests/unit/contexts/world/domain/aggregates/
  - Pre-existing mypy errors in coordinates.py and event_bus.py are not from new code
---

## Mon Feb 16 17:20 CST 2026 - SIM-003
- Created Calendar API router with GET and POST endpoints
- Implemented: GET /calendar/{world_id}, POST /calendar/{world_id}/advance
- Request/Response models defined in router: CalendarResponse, AdvanceCalendarRequest
- Router registered in both src.api.main_api_server and src.api.app
- In-memory storage pattern used (MVP implementation)
- Files changed:
  - src/api/routers/calendar.py (new)
  - src/api/main_api_server.py (modified - added router import and include_router)
  - src/api/app.py (modified - added router import and include_router)
  - tests/api/routers/__init__.py (new)
  - tests/api/routers/test_calendar.py (new)
- **Learnings for future iterations:**
  - There are TWO app factories: src.api.main_api_server:create_app and src.api.app:create_app
  - Tests import `api_server` which uses src.api.app, so routers must be registered in both places
  - Error responses use ErrorDetail schema with 'code', 'message', 'details' fields (not 'detail')
  - The 404 handler in src.api.errors catches ALL 404s and reformats them with generic message
  - Result.unwrap() returns T but mypy still sees it as T | None due to type stub limitations
  - Pre-existing test failures in test_world_state_aggregate.py (uses old world_time field)
---

## Mon Feb 16 19:45 CST 2026 - SIM-004
- Created CalendarDisplay React component with TanStack Query hooks
- Implemented: useCalendar(worldId) and useAdvanceCalendar() hooks with optimistic updates
- Added CalendarResponse and AdvanceCalendarRequest schemas to frontend types
- Component uses shadcn/ui Card, Button components
- Replaced hardcoded WorldClock component with CalendarDisplay in WorldPage
- Accessibility: aria-live regions for announcements, aria-labels on all buttons
- Loading skeleton, error state with retry, and success state implemented
- E2E tests for rendering, button interactions, error handling, and accessibility
- Files changed:
  - frontend/src/types/schemas.ts (modified - added CalendarResponse, AdvanceCalendarRequest)
  - frontend/src/lib/api/calendarApi.ts (new - TanStack Query hooks)
  - frontend/src/lib/api/index.ts (modified - exported calendar hooks)
  - frontend/src/components/world/CalendarDisplay.tsx (new)
  - frontend/src/features/world/WorldPage.tsx (modified - replaced WorldClock with CalendarDisplay)
  - frontend/tests/e2e/calendar.spec.ts (new - E2E tests)
- **Learnings for future iterations:**
  - Frontend TanStack Query pattern: queryKeys object for cache key factory
  - Optimistic updates use onMutate (save previous, update cache), onError (rollback), onSettled (invalidate)
  - E2E tests import Page type from '@playwright/test' at top level (not inline) to avoid ESLint parsing errors
  - Component uses data-testid and data-state for test assertions
  - Screen reader announcements use .sr-only class with aria-live="polite"
---
## Mon Feb 16 20:15 CST 2026 - SIM-005
- Enhanced HistoryEvent entity with simulation support fields
- Added ImpactScope enum: LOCAL, REGIONAL, GLOBAL
- Added EventType values: TRADE, NATURAL (EventType already existed with many values)
- Added nullable backward-compatible fields: impact_scope, affected_faction_ids, affected_location_ids, structured_date
- Updated to_dict() serialization for new fields
- All 32 HistoryEvent tests pass
- Files changed:
  - src/contexts/world/domain/entities/history_event.py (modified)
  - tests/unit/contexts/world/domain/test_world_entities.py (modified - added 11 new tests)
- **Learnings for future iterations:**
  - EventType enum already existed with extensive values - added TRADE and NATURAL
  - Used TYPE_CHECKING pattern for forward reference to WorldCalendar to avoid circular import
  - New fields use Optional[Type] = None pattern for backward compatibility
  - Tests for this entity are in test_world_entities.py, not a separate file
  - to_dict() needs explicit handling for optional enums (check None before .value)
  - to_dict() needs explicit handling for structured objects (call .to_dict() if not None)
---
## Mon Feb 16 21:30 CST 2026 - SIM-006
- Created Events API router with GET and POST endpoints
- Implemented: GET /world/{world_id}/events (list with pagination/filters), POST /world/{world_id}/events (create), GET /world/{world_id}/events/{event_id} (details)
- Added schemas to src/api/schemas.py: HistoryEventResponse, CreateEventRequest, EventListResponse
- Filter parameters: event_type, impact_scope, from_date, to_date, faction_id, location_id, is_secret, page, page_size
- Router added to existing events.py file (which already had SSE streaming endpoints)
- In-memory storage pattern used (MVP implementation)
- All 22 new tests pass
- Files changed:
  - src/api/routers/events.py (modified - added history event endpoints)
  - src/api/schemas.py (modified - added HistoryEventResponse, CreateEventRequest, EventListResponse)
  - tests/api/routers/test_history_events.py (new - 22 tests)
- **Learnings for future iterations:**
  - events.py router was already registered in main_api_server.py and app.py - no registration changes needed
  - Router paths start with /world/{world_id}/events and get /api prefix from include_router
  - Error responses use ErrorDetail schema with 'code', 'message', 'details' fields (not 'detail')
  - The global 404 handler catches ALL 404s including HTTPException with 404 status
  - Test file named test_history_events.py to distinguish from SSE events tests
  - Use reset_events_storage() fixture for test isolation
---
## Mon Feb 16 22:00 CST 2026 - SIM-007
- Created WorldTimeline React component with TanStack Query hooks
- Implemented: useWorldEvents(worldId, filters) and useCreateEvent() hooks with cache invalidation
- Added HistoryEventResponse, EventListResponse, EventFilterParams, CreateEventRequest schemas to frontend types
- Component uses shadcn/ui Card, Badge, Button, Select, Skeleton, ScrollArea components
- Vertical timeline with event cards showing: title, date, type badge, impact scope badge, description (expandable)
- Filter controls: event_type dropdown, impact_scope dropdown with clear button
- Accessibility: role='feed' on timeline, aria-labelledby on event cards, labels on filter dropdowns
- Empty state, error state with retry, loading state with skeleton
- E2E tests for rendering, filters, empty state, error handling, accessibility
- Files changed:
  - frontend/src/types/schemas.ts (modified - added HistoryEventResponse, EventListResponse, EventFilterParams, CreateEventRequest schemas)
  - frontend/src/lib/api/eventsApi.ts (new - TanStack Query hooks for events API)
  - frontend/src/lib/api/index.ts (modified - exported events hooks)
  - frontend/src/components/world/WorldTimeline.tsx (new - timeline component)
  - frontend/tests/e2e/timeline.spec.ts (new - E2E tests)
- **Learnings for future iterations:**
  - shadcn/ui components in this codebase use two paths: @/components/ui/* and @/shared/components/ui/*
  - Badge and Skeleton are in @/shared/components/ui/, Card and Button are in @/components/ui/
  - ScrollArea is in @/components/ui/scroll-area (kebab-case)
  - Use role='feed' for timeline containers per ARIA best practices
  - Article elements have implicit role="article" - don't add explicit role
---
## Mon Feb 16 22:30 CST 2026 - SIM-008
- Created DiplomaticStatus enum for diplomatic relationship statuses
- Implemented: 6 status values (ALLIED, FRIENDLY, NEUTRAL, COLD, HOSTILE, AT_WAR) with strength-based mapping
- Added from_relation_strength(strength: int) class method with clamping (-100 to 100)
- Added color and label properties for UI rendering
- Color mapping uses Tailwind-compatible hex colors (green -> red gradient)
- All 41 tests pass
- Files changed:
  - src/contexts/world/domain/value_objects/diplomatic_status.py (new)
  - src/contexts/world/domain/value_objects/__init__.py (modified - added export)
  - tests/unit/contexts/world/domain/value_objects/test_diplomatic_status.py (new - 41 tests)
- **Learnings for future iterations:**
  - Enum properties use simple dict lookups for color and label mappings
  - Strength clamping uses max(-100, min(100, strength)) pattern
  - Boundary tests are crucial for strength-based enum mappings
  - Pre-existing mypy errors in coordinates.py are not from new code
---
## Mon Feb 16 23:00 CST 2026 - SIM-009
- Created DiplomacyMatrix aggregate to manage faction diplomatic relations
- Implemented: get_status, set_status (with Result pattern), get_allies, get_enemies, get_neutral, to_matrix
- Relations stored as symmetric tuples (A,B) where tuple is always sorted for consistency
- Added register_faction and remove_faction for faction lifecycle management
- Added get_factions_by_status for filtering by specific status
- Validation: self-relations rejected, unknown factions in relations detected
- All 33 tests pass
- Files changed:
  - src/contexts/world/domain/aggregates/diplomacy_matrix.py (new)
  - src/contexts/world/domain/aggregates/__init__.py (new)
  - tests/unit/contexts/world/domain/aggregates/test_diplomacy_matrix.py (new - 33 tests)
- **Learnings for future iterations:**
  - Aggregate directory needed __init__.py (didn't exist before)
  - Relations use sorted tuple keys for O(1) lookup regardless of (A,B) or (B,A) order
  - to_matrix() fills missing relations with NEUTRAL and self-relations as "-"
  - Aggregate inherits from Entity, so gets id, version, created_at, updated_at automatically
  - Use self.touch() when modifying aggregate state to update timestamp and version
---
## Mon Feb 16 23:30 CST 2026 - SIM-010
- Created Diplomacy API router with 3 endpoints
- Implemented: GET /world/{world_id}/diplomacy (full matrix), GET /world/{world_id}/diplomacy/faction/{faction_id} (single faction), PUT /world/{world_id}/diplomacy/{faction_a}/{faction_b} (set relation)
- Request/Response models defined in router: DiplomacyMatrixResponse, FactionDiplomacyResponse, SetRelationRequest
- Router registered in both src.api.main_api_server and src.api.app
- In-memory storage pattern used (MVP implementation)
- Status validation with case-insensitive and whitespace-tolerant parsing
- All 17 tests pass
- Files changed:
  - src/api/routers/diplomacy.py (new)
  - src/api/main_api_server.py (modified - added router import and include_router)
  - src/api/app.py (modified - added router import and include_router)
  - tests/api/routers/test_diplomacy.py (new - 17 tests)
- **Learnings for future iterations:**
  - Schemas can be defined directly in router file (not required to be in schemas.py for smaller routers)
  - Status validation uses case-insensitive comparison with .lower().strip()
  - Router registered in BOTH main_api_server.py and app.py for test compatibility
  - Tests use reset_diplomacy_storage() fixture for test isolation
---
## Mon Feb 17 10:45 CST 2026 - SIM-011
- Created DiplomacyMatrix React component with TanStack Query hooks
- Implemented: useDiplomacyMatrix(worldId), useFactionDiplomacy(), useSetRelation() hooks with optimistic updates
- Added DiplomaticStatusEnum, DiplomacyMatrixResponse, FactionDiplomacyResponse, SetRelationRequest schemas to frontend types
- Component uses shadcn/ui Card, Dialog, Select, Tooltip components
- Grid with color-coded cells: ALLIED=#22c55e, FRIENDLY=#84cc16, NEUTRAL=#eab308, COLD=#f97316, HOSTILE=#ef4444, AT_WAR=#dc2626
- Click cell opens dialog to edit relation with status dropdown
- Keyboard navigation: Arrow keys navigate cells, Enter opens dialog
- Loading skeleton, error state with retry, empty state, success state implemented
- E2E tests for rendering, tooltip, dialog, keyboard navigation, accessibility
- TypeScript passes, ESLint passes (2 warnings acceptable)
- Files changed:
  - frontend/src/types/schemas.ts (modified - added DiplomaticStatusEnum, DiplomacyMatrixResponse, FactionDiplomacyResponse, SetRelationRequest)
  - frontend/src/lib/api/diplomacyApi.ts (new - TanStack Query hooks)
  - frontend/src/lib/api/index.ts (modified - exported diplomacy hooks)
  - frontend/src/components/world/DiplomacyMatrix.tsx (new)
  - frontend/tests/e2e/diplomacy.spec.ts (new - E2E tests)
- **Learnings for future iterations:**
  - Grid role requires tabIndex={0} for keyboard focusability (eslint jsx-a11y/interactive-supports-focus)
  - Scope attribute is only valid on th elements, not div elements with role="columnheader"/"rowheader"
  - Use Fragment instead of empty <> when key is needed in map
  - Labels without associated inputs should use <span> instead of <label>
  - useCallback dependencies need to include functions used in the callback
---
## Mon Feb 17 12:55 CST 2026 - SIM-012
- Created FactionIntent entity with frozen dataclass for immutability
- Implemented: IntentType enum (7 values: EXPAND, ATTACK, DEFEND, ALLY, TRADE, RECOVER, CONSOLIDATE)
- Implemented: is_offensive property (ATTACK, EXPAND), is_defensive property (DEFEND, CONSOLIDATE)
- Added to_dict() and from_dict() serialization methods with datetime parsing
- Added validation: empty faction_id, empty narrative, priority range (1-10)
- All 36 tests pass
- Files changed:
  - src/contexts/world/domain/entities/faction_intent.py (new)
  - src/contexts/world/domain/entities/__init__.py (modified - added exports)
  - tests/unit/contexts/world/domain/entities/test_faction_intent.py (new - 36 tests)
- **Learnings for future iterations:**
  - Frozen dataclasses are hashable and can be used in sets/dicts
  - For frozen dataclass equality, all fields must match (including created_at)
  - Test equality with frozen dataclasses requires same timestamp for created_at field
  - isinstance check for typed enum fields is redundant and causes mypy unreachable error
---
## Mon Feb 17 13:30 CST 2026 - SIM-013
- Created FactionIntentGenerator application service
- Implemented rule-based intent generation with 6 rules evaluated in order
- Rules: RECOVER (wealth<20), ATTACK (enemies+military>=30), ALLY (isolated+wealth>40), EXPAND (territories<3+wealth>50), DEFEND (military>80+enemies), CONSOLIDATE (default)
- Max 3 intents returned per faction, sorted by priority descending
- Edge case: disbanded faction returns empty list
- All 22 tests pass
- Files changed:
  - src/contexts/world/application/services/faction_intent_generator.py (new)
  - tests/unit/contexts/world/application/services/test_faction_intent_generator.py (new - 22 tests)
- **Learnings for future iterations:**
  - Application services directory is at src/contexts/world/application/services/
  - Test directory structure: tests/unit/contexts/world/application/services/
  - Faction uses economic_power for wealth, military_strength for military, territories list for territory count
  - DiplomacyMatrix.get_enemies() returns list of faction IDs with HOSTILE or AT_WAR status
  - DiplomacyMatrix.get_allies() returns list of faction IDs with ALLIED status
  - DiplomacyMatrix.get_neutral() returns list of faction IDs with NEUTRAL status
  - For MVP, target selection (weakest enemy, wealthiest neutral) needs faction repository access - currently picks first available
---
## Mon Feb 17 14:10 CST 2026 - SIM-014
- Created SimulationTick value object with frozen dataclass
- Created supporting value objects:
  - ResourceChanges: wealth_delta, military_delta, influence_delta (-100 to 100)
  - DiplomacyChange: faction_a, faction_b, status_before, status_after
- Properties added:
  - ResourceChanges.has_changes, total_change
  - DiplomacyChange.is_significant (detects positive/negative crossings)
  - SimulationTick.has_changes, total_resource_changes, total_diplomacy_changes
- All 44 tests pass
- Files changed:
  - src/contexts/world/domain/value_objects/simulation_tick.py (new)
  - src/contexts/world/domain/value_objects/__init__.py (modified - added exports)
  - tests/unit/contexts/world/domain/value_objects/test_simulation_tick.py (new - 44 tests)
- **Learnings for future iterations:**
  - Frozen dataclasses with list/dict fields are NOT hashable (lists are mutable)
  - Use tuples for true immutability if hashability is needed
  - WorldCalendar has to_dict() and from_dict() methods for serialization
  - DiplomaticStatus enum values can be constructed from string values
---
## Mon Feb 17 15:30 CST 2026 - SIM-015
- Created WorldSimulationService for basic simulation tick preview
- Implemented advance_simulation(world_id, days) method with Result pattern
- Added custom exception types: SimulationError, WorldNotFoundError, InvalidDaysError, RepositoryError
- Created Protocol interfaces for repository dependencies (IWorldStateRepository, IFactionRepository)
- Built _build_diplomacy_matrix() method to convert Faction relations to DiplomacyMatrix
- Days validation enforces 1-365 range
- Preview mode: loads world, advances calendar, generates faction intents, but does NOT apply changes
- All 22 unit tests pass covering:
  - Successful preview with various day counts (1, 7, 30, 365)
  - Invalid days errors (0, negative, exceeds max)
  - World not found error
  - Repository error handling
  - Empty world (no factions)
  - All disbanded factions
  - Diplomacy matrix building from faction relations
  - Tick data integrity
- Files changed:
  - src/contexts/world/application/services/world_simulation_service.py (new)
  - tests/unit/contexts/world/application/services/test_world_simulation_service.py (new - 22 tests)
- **Learnings for future iterations:**
  - Use Protocol classes with @runtime_checkable for repository interfaces
  - DiplomacyMatrix can be built from Faction.relations using DiplomaticStatus.from_relation_strength()
  - Preview mode returns SimulationTick without applying changes to world
  - Calendar advance returns Result type that needs proper error unwrapping
  - Use AsyncMock for async repository methods in tests
---
## Mon Feb 17 17:30 CST 2026 - SIM-016
- Created ResolutionResult dataclass for tracking intent resolution outcomes
- Implemented _resolve_intents() method with all resolution rules
- Resolution rules implemented:
  - ATTACK: Reduces military (-5 each), transfers territory if attacker stronger, conflict detection for multiple attackers
  - EXPAND: Requires economic_power >= 20, costs 10 wealth
  - ALLY: Creates ALLIED diplomacy status, fails if currently AT_WAR/HOSTILE
  - RECOVER: +5 wealth if below 50
  - DEFEND: +3 military if below 80
  - CONSOLIDATE: +2 wealth, +1 military (default fallback)
  - TRADE: Succeeds with no immediate effect (placeholder for future trade routes)
- Conflict handling: Multiple attackers on same target - strongest wins territory, all suffer losses
- ResolutionResult tracks: resource_changes, diplomacy_changes, territory_changes, successful_intents, failed_intents
- All 35 unit tests pass covering:
  - ResolutionResult dataclass methods (12 tests)
  - ATTACK intent scenarios (6 tests)
  - EXPAND intent scenarios (2 tests)
  - ALLY intent scenarios (3 tests)
  - RECOVER intent scenarios (2 tests)
  - DEFEND intent scenarios (2 tests)
  - CONSOLIDATE intent (1 test)
  - TRADE intent (1 test)
  - Priority handling (2 tests)
  - Edge cases (4 tests)
- Files changed:
  - src/contexts/world/application/services/world_simulation_service.py (modified - added ResolutionResult, _resolve_intents, helper methods)
  - tests/unit/contexts/world/application/services/test_intent_resolution.py (new - 35 tests)
- **Learnings for future iterations:**
  - Faction() default constructor fails validation - must provide name field
  - Use helper function instead of lambda when type narrowing is needed for mypy
  - Intent conflict resolution happens BEFORE military losses to ensure all attackers take damage
  - ResourceChanges accumulation uses clamping to -100/100 range
  - Territory changes recorded as dict: location_id -> new_owner_faction_id
---