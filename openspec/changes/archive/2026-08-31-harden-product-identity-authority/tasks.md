# Tasks

- [x] 1. Add fixture-based red tests for duplicate package authorities,
      malformed identity, stale documentation projections, retired identity,
      and OpenAPI drift.
- [x] 2. Replace version literals and frontend fallbacks with a strict shared
      product-identity reader and build projection.
- [x] 3. Make the SSOT gate derive expectations from the server manifest and
      verify the explicit current projections.
- [x] 4. Remove dynamic release numbers from contributor/quickstart prose and
      correct current Node and Owner-only session projections.
- [x] 5. Run identity tests, build checks, server/frontend quality gates, and
      strict OpenSpec validation; complete independent standards and
      specification review before archiving the change.
