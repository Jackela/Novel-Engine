# Tasks

- [x] 1. Replace the unconditional Lore injection requirement with lifecycle
      gating and budgeted progressive-disclosure scenarios.
- [x] 2. Add a cross-boundary regression test that keeps the configuration
      default equal to the application default.
- [x] 3. Give the frontend parser and selector one exhaustive lifecycle-status
      definition derived from the generated API union.
- [x] 4. Update the Lorebook projection and the repository OpenSpec skill to
      the active lifecycle, budget, HTTP surface, and capability authority.
- [x] 5. Run focused Lore tests, frontend checks, server gates, and strict
      OpenSpec validation; complete independent standards and specification
      review before archiving the change.
