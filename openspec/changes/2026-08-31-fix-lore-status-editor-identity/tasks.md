# Tasks

- [x] 1. Add the Lore editor requirement and validate the dated OpenSpec
      change with strict validation.
- [x] 2. Add a failing component regression for A-to-B draft isolation and a
      deferred-Promise assertion for completion-time focus restoration.
- [x] 3. Add action-level coverage for target-specific success, pending, and
      failure behavior.
- [x] 4. Narrow the Inspector Lore model and make the public Lore module own
      its document-keyed private form.
- [x] 5. Return the real Lore save Promise from the page-model adapter without
      changing the existing mutation owner.
- [x] 6. Run focused tests, frontend quality/build checks, repository gates,
      full server tests, and strict OpenSpec validation; review the final diff.
