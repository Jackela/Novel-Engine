# Tasks

- [ ] 1. Remove the guest principal: session surface, auth store scoping,
      entry flow, frontend guest entry, spec scenarios; drop guest columns
- [ ] 2. Volume hierarchy: migration + volumes CRUD surface + volume-scoped
      chapter reorder + default volume seeding (projects, import)
- [ ] 3. Chapter ↔ outline-beat association (link, read, generation wiring)
- [ ] 4. Resident context assembly (outline position, rolling summary,
      recent-chapter tail) wired into the proposal pipeline
- [ ] 5. Keyword-triggered lore entries (document-as-entry, alias metadata,
      key matching over resident context + manuscript)
- [ ] 6. LLM editorial review over snapshots with the closed dimension set;
      retire the word-count findings
- [ ] 7. Project usage aggregation endpoint
- [ ] 8. Whole-book generation loop in the Studio (stoppable, resumable,
      auto-accept per chapter)
- [ ] 9. Regenerate the OpenAPI baseline + frontend api-types for every
      route-affecting slice
- [ ] 10. Browser e2e coverage for the volume-aware library and the
      whole-book loop
